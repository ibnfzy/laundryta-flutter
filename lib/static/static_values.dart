import 'package:flutter/material.dart';

class StaticValue {
  const StaticValue._();

  static const primaryColor = Color.fromARGB(255, 160, 214, 180);
  static const secondaryColor = Colors.white;
}
